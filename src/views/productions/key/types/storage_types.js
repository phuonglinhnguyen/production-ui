import { NAMESPACE_MODULE } from '../constants';
export const STORAGE_FUNCION = `${NAMESPACE_MODULE}_STORAGE_FIELD`;
export const ADD = `${STORAGE_FUNCION}_ADD`;
export const RESET = `${STORAGE_FUNCION}_RESET`;

export default {
    ADD,
    RESET,
}

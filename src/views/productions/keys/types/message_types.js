import { NAMESPACE_MODULE } from '../constants';
export const MESSAGE = `${NAMESPACE_MODULE}_MESSAGE`;
export const RESET = `${MESSAGE}_RESET`;
export const ADD = `${MESSAGE}_ADD`;

export default {
    ADD,
    RESET,
}